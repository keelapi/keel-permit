# Input artifacts — 01-02-permit-with-custom-metadata

This directory will contain:

- `permit.json` — a single Permit (`decision: "allow"`) with `custom_metadata: {"shadow_override": {"outcome": "blocked"}}`.
- `export.jsonl` — newline-delimited records forming a minimal export bundle (genesis chain entry + permit-creation event + permit + closure record).
- `manifest.json` — content-hashed manifest.
- `signature.bin` — detached Ed25519 signature over `manifest.json` UTF-8 bytes.
- `trust-root.json` — test-only trust root containing the public key used for signing.

**Currently TODO.** These files must be generated by the fixture-generator tool (see `test-vectors/tools/` — scaffolding pending). The permit MUST include `custom_metadata: {"shadow_override": {"outcome": "blocked"}}` to exercise the extension carrier conformance requirement introduced in v1.5.0.
