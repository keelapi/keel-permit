# pinned-valid-incident-v2-bundle

Pinned sibling of `valid-incident-v2-bundle`. The pack adds `claim_set` and inline `semantics_pins` for the requested claim semantics; evidence bytes are otherwise copied from the parent fixture.
