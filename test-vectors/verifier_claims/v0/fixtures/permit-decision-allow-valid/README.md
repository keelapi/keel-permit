# permit-decision-allow-valid

Valid signed permit allow decision.

## What It Tests

The pack contains a signed issuance-time permit decision binding whose canonical payload decision is `allow`.

## Expected Verdict

`permit.decision.v1` is expected to return `supported`.
