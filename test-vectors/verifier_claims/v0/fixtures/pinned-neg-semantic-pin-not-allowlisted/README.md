# pinned-neg-semantic-pin-not-allowlisted

Pinned negative sibling of `valid-export-hash-signature`. Pinned a well-formed keel.export_manifest.integrity.v1 artifact whose bytes self-hash correctly but whose (id, hash) pair is not in the permanent allowlist.
