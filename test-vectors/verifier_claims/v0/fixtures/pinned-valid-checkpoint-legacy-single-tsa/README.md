# pinned-valid-checkpoint-legacy-single-tsa

Pinned sibling of `valid-checkpoint-legacy-single-tsa`. The pack adds `claim_set` and inline `semantics_pins` for the requested claim semantics; evidence bytes are otherwise copied from the parent fixture.
