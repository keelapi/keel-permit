# pinned-neg-required-semantic-pin-missing

Pinned negative sibling of `valid-export-hash-signature`. Added claim_set for export.integrity.v1 but omitted the required export-manifest semantic artifact from semantics_pins.
