# pinned-valid-checkpoint-multi-tsa-receipts

Pinned sibling of `valid-checkpoint-multi-tsa-receipts`. The pack adds `claim_set` and inline `semantics_pins` for the requested claim semantics; evidence bytes are otherwise copied from the parent fixture.
