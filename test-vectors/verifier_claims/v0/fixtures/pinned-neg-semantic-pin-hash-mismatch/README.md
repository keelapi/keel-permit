# pinned-neg-semantic-pin-hash-mismatch

Pinned negative sibling of `valid-export-hash-signature`. Pinned the released export-manifest semantic artifact bytes while declaring an all-zero sha256 hash, producing a top-level integrity error.
