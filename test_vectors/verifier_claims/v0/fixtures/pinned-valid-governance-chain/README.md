# pinned-valid-governance-chain

Pinned sibling of `valid-governance-events-json`. The pack adds `claim_set` and inline `semantics_pins` for the requested claim semantics; evidence bytes are otherwise copied from the parent fixture.
