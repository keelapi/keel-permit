# pinned-valid-export-integrity

Pinned sibling of `valid-export-hash-signature`. The pack adds `claim_set` and inline `semantics_pins` for the requested claim semantics; evidence bytes are otherwise copied from the parent fixture.
