# pinned-valid-closure-v2-dispatch-binding

Pinned sibling of `valid-closure-v2-dispatch-binding`. The pack adds `claim_set` and inline `semantics_pins` for the requested claim semantics; evidence bytes are otherwise copied from the parent fixture.
